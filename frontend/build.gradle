plugins {
  id "com.moowork.node" version "1.2.0"
}

node {
  download = false
}

task npmBuild(type: NpmTask) {
  args = ['run', 'build']
}

npmBuild.dependsOn(npmInstall)

task build {
  dependsOn(npmBuild)
}
