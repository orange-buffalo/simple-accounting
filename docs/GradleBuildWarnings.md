# Gradle Build Warnings

Generated from:

```bash
./gradlew clean :app:compileKotlin :app:compileTestKotlin --warning-mode=all --console=plain --no-build-cache
```

The command completed successfully and did not run tests. Because the environment appears to be CI-like, `:app:compileKotlin` also triggered the frontend production build through `copyFrontend`.

| Warning / type | Source / cause / reason | Potential fix | Fix complexity |
| --- | --- | --- | --- |
| Unsupported Kotlin plugin version in `buildSrc` | `buildSrc/build.gradle.kts` applies `kotlin-dsl` and also applies `alias(libs.plugins.kotlin.serialization)`. Gradle 9.5 embeds Kotlin `2.3.20`, while the build requests Kotlin serialization plugin `2.3.21`; Gradle warns that mixing Kotlin versions in a Kotlin DSL build can cause incompatibilities. | Align Kotlin plugin versions used by `buildSrc` with Gradle's embedded Kotlin version, or avoid applying a mismatched Kotlin plugin in `buildSrc`. If `kotlinx.serialization` compiler plugin is required there, pin it to the embedded-compatible version or wait for a Gradle/Kotlin version alignment. | Medium |
| `SaHotReloadPlugin: No test config file found ... hot reload is not enabled` | `buildSrc/src/main/kotlin/SaHotReloadPlugin.kt:55` prints to stdout when `app/src/test/.test-config.yaml` is absent. This is an informational local-environment message, not a compiler or Gradle deprecation warning. | Reduce noise by logging only when Gradle info logging is enabled, by creating a local `.test-config.yaml` from the template, or by making missing config silent because disabled hot reload is already the default. | Trivial |
| `Tests config file not found. Using default port: 9393` | `frontend/build-config/proxy-port-resolver.ts:9` prints during Vite config evaluation when `app/src/test/.test-config.yaml` is absent. It appears during frontend build because `copyFrontend` is wired into Kotlin compilation in CI-like runs. | Make this message debug-only or silent for production builds, or create the local test config file. | Trivial |
| Vite / Rolldown `[PLUGIN_TIMINGS] Warning` for `svg-loader` | Frontend production build reports plugin timing: `svg-loader` accounts for most plugin time. Source is `frontend/build-config/vite-plugins.ts`, where `vite-svg-loader` runs SVGO over SVG imports. This is a build-performance diagnostic, not a correctness issue. | Investigate SVG import volume and `vite-svg-loader`/SVGO cost. Potential options include disabling SVGO where unnecessary, pre-optimizing SVGs, upgrading `vite-svg-loader`, or narrowing SVG loader usage. | Requires research |
| Deprecated Reactor `Context.putAll(Context)` | `app/src/main/kotlin/io/orangebuffalo/simpleaccounting/infra/graphql/SaGraphQLContextFactory.kt:38` calls `currentReactorContext.putAll(ReactiveSecurityContextHolder.withAuthentication(authentication))`. Reactor deprecated the overload accepting `Context`; the replacement is the `ContextView`-based API. | Call the non-deprecated overload by treating the authentication context as `ContextView`, or otherwise compose Reactor contexts with the current Reactor API. Verify GraphQL authentication context propagation after the change. | Medium |
| `:buildSrc:jar: No valid plugin descriptors were found in META-INF/gradle-plugins` | `buildSrc` contains plugin implementation classes but no Gradle plugin descriptors. The build applies these classes directly from `buildSrc` using `apply<SaJooqCodeGenPlugin>()`, etc., so descriptors are not required. | Leave as-is if direct `buildSrc` class application is intentional. If the project wants conventional plugin IDs, add descriptors or precompiled script plugins. | Medium |
| `Consider enabling configuration cache` | Gradle emits this optimization suggestion at the end of the build. It is not a warning. Configuration cache may be blocked by custom `buildSrc` plugins/tasks or frontend task implementations. | Run Gradle with configuration-cache problem reporting and fix incompatible custom task/plugin patterns if faster local/CI configuration is a priority. | Requires research |
