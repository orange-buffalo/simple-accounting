# Spring Boot Test Coverage Assessment

This inventory includes Spring Boot tests that extend `SaIntegrationTestBase`, excluding full stack tests that extend
`SaFullStackTestBase` and GraphQL API tests under `business/api` or `infra/graphql`. The two document upload/download
REST endpoint tests under `business/api/documents` are included because they are not GraphQL API tests.

The mock/spy bean column tracks Spring test bean overrides defined in the test class, such as `@MockitoBean`,
`@MockBean`, `@SpyBean`, or `@MockitoSpyBean`. The legacy security column tracks deprecated mock security annotations
from `SecurityMocks.kt`, including `@WithSaMockUser` and the `@WithMock*User` shortcuts.

`@WithMock*User` and `@WithSaMockUser` only populate Spring Security context. They do not create database users. The
tests that use them either create the matching users through `preconditions` / `lazyPreconditions` / `EntitiesFactory`,
or do not require a persisted user for the asserted path. Those annotations are therefore tracked only in the legacy
security column, not as database manipulation outside the preconditions API.

## Planned Migration

| Test | Mock/spy beans in test class | Manipulates database entities outside preconditions API | Uses legacy mock security annotations | Action | Coverage assessment |
| --- | --- | --- | --- | --- | --- |
| `business/integration/downloads/DownloadsServiceTest` | No | No | No | Remove after confirming API/document download coverage for unknown-token and provider paths | Mostly covered by `CreateDocumentDownloadUrlMutationTest`, `DocumentsContentApiTest`, and full stack document download tests. Unknown-token and provider delegation can be covered at API level, but this service test gives cheaper and clearer coverage of token resolution internals. |

## Migrated

| Test | Mock/spy beans in test class | Manipulates database entities outside preconditions API | Uses legacy mock security annotations | Migration result | Coverage assessment |
| --- | --- | --- | --- | --- | --- |
| `business/api/documents/DocumentsContentApiTest` | No | No | No | Removed mocked `DownloadsService`; test now creates real download tokens and reads content from `TestDocumentsStorage` | Partly covered by higher-level document download flows such as `DocumentsOverviewFullStackTest` and `DocumentDownloadByWorkspaceTokenFullStackTest`. The anonymous token endpoint and 404 mapping are REST boundary behavior; they are now covered without a test bean override. |
| `business/documents/DocumentsServiceTest` | No | No | Yes, `@WithMockFryUser` | Removed test class | Partly covered by document storage status/download API and full stack flows. The removed service-level delegation checks were narrow and mostly redundant with user-visible document behavior coverage. |
| `business/documents/storage/gdrive/GoogleDriveDocumentsStorageServiceTest` | Yes, `@MockitoBean` for OAuth and push notification collaborators | Yes, used `JdbcAggregateTemplate.deleteAll` and `save` for Google Drive integration records | Yes, `@WithSaMockUser` | Removed test class | Mostly covered by `UserProfileGoogleDriveDocumentStorageFullStackTest` plus Google Drive upload/download component tests. Remaining protocol details, push-notification behavior, and authorization failure handling depended on Google Drive/OAuth mocks and were not kept as Spring Boot service tests. |
| `business/expenses/ExpenseServiceTest` | No | No | No | Removed `@WithMockFryUser`; service calls now run under `runAs(preconditions.fry.toSecurityPrincipal())` | Representative scenarios are covered by `CreateExpenseMutationTest`, `EditExpenseMutationTest`, `CreateExpenseFullStackTest`, and `EditExpenseFullStackTest`. The full calculation matrix for default/foreign currency, business percentage, tax, and conversion status is not fully covered higher; adding all cases there would be slow and noisy, so the matrix should not be duplicated only at UI/API level. |
| `business/generaltaxes/GeneralTaxesReportingServiceTest` | No | No | No | Removed test class | Covered at a higher level by `ReportingFullStackTest`, which exercises general tax report combinations, finalized-only behavior, and pending items. The removed service test was largely redundant with that user-visible report coverage. |
| `business/incomes/IncomesServiceTest` | No | No | No | Removed `@WithMockFryUser`; service calls now run under `runAs(preconditions.fry.toSecurityPrincipal())` | Representative scenarios are covered by `CreateIncomeMutationTest`, `EditIncomeMutationTest`, `CreateIncomeFullStackTest`, `EditIncomeFullStackTest`, and `IncomesOverviewFullStackTest`. The detailed conversion/tax matrix and linked-invoice side effects are not exhaustively covered higher; full higher-level duplication would add considerable cost. |
| `business/invoices/InvoicesServiceTest` | No | No | No | Removed `@WithMockFryUser`; test now runs `saveInvoice` inside a real `runAs(testData.fry.toSecurityPrincipal())` block | Partly covered by `CreateInvoiceMutationTest`, `EditInvoiceMutationTest`, `CancelInvoiceMutationTest`, `InvoicesQueryTest`, and invoice full stack tests. Overdue transition logic is business-rule heavy and not fully exercised higher; focused service coverage remains lower cost and easier to diagnose. |
| `business/security/jwt/RefreshTokensServiceTest` | No | No | No | Kept as focused service coverage | Covered in user-visible form by `RefreshAccessTokenMutationTest` and `InvalidateRefreshTokenMutationTest`. Token generation, persistence lookup, expiration, and prolongation internals are not all covered higher; keeping focused service coverage avoids forcing cookie/API tests to validate persistence details. |
| `business/workspaces/WorkspacesServiceTest` | No | No | Yes, `@WithMock*User` and `@WithSaMockUser` | Removed test class | Partly covered by workspace GraphQL tests and full stack workspace-token flows such as `WorkspaceTokenAccessWalkThroughFullStackTest` and `WorkspacesOverviewFullStackTest`. The deleted test contained the complete access-mode matrix across owner, regular users, transient workspace-token users, expired tokens, revoked tokens, and other workspaces. |
| `infra/oauth2/DbReactiveOAuth2AuthorizedClientServiceTest` | Yes, `@MockitoBean` for client registration repository | No | No | Removed test class | Indirectly covered by Google Drive profile and OAuth full stack flows. The persistence details for authorized clients, including refresh-token preservation on updates, were not easily asserted at a higher level without coupling tests to infrastructure state. |
| `infra/oauth2/OAuth2ClientAuthorizationProviderTest` | Yes, `@MockitoBean` for auth event listener and saved authorization request repository | No | Yes, `@WithSaMockUser` | Removed test class | Partly covered by `CompleteOAuth2FlowMutationTest` and Google Drive profile authorization flows. Authorization URL construction, token endpoint parameters, event emission, and failure modes were infrastructure contracts; higher-level coverage exists for happy/error flows. |
| `infra/oauth2/OAuth2WebClientBuilderProviderTest` | No | Yes, inserted authorized-client rows through `JdbcAggregateTemplate.insert` | Yes, `@WithSaMockUser` | Removed test class | Indirectly covered by Google Drive storage/profile flows. Access-token reuse, missing authorization cleanup, and refresh-token renewal were infrastructure behaviors with broad mocks and timing-sensitive setup. |

## Will Keep Without Migration

| Test | Mock/spy beans in test class | Manipulates database entities outside preconditions API | Uses legacy mock security annotations | Coverage assessment |
| --- | --- | --- | --- | --- |
| `business/api/documents/DocumentsUploadApiTest` | No | Yes, stores upload tokens through `TokensRepository.storeToken` | No | Partly covered by full stack upload scenarios in `SaDocumentsUploadFullStackTest`, `SaDocumentsUploadLocalFileSystemFullStackTest`, `SaDocumentsUploadGoogleDriveFullStackTest`, and entity create/edit tests with attachments. Token-not-found, anonymous upload access, binary content type, and workspace association are lower-level REST upload contract checks; some could be covered higher, but with more setup and weaker failure localization. |
| `business/common/pesistence/TimestampsTest` | No | No | No | Not meaningfully covered by higher-level tests. It verifies database timestamp-with-time-zone persistence mapping; covering it through API or UI flows would be indirect and would not clearly isolate mapping regressions. |
| `business/security/authentication/BruteForceDefenseTest` | No | Yes, updates login statistics through `PlatformUsersRepository.save` | No | Basic bad-credentials behavior is covered by `CreateAccessTokenByCredentialsMutationTest`. Progressive lock timing, lock expiration boundaries, cap behavior, and parallel request throttling are not fully covered higher; they are technically possible through GraphQL login tests, but would be slower and more timing-sensitive. |
| `business/ui/WebUiConfigTest` | No | No | No | Full stack tests implicitly verify that the app shell loads. Favicon/static resource serving, SPA route fallback, anonymous access, and cache headers are HTTP configuration details not directly asserted higher; they can be covered with endpoint tests more cleanly than browser tests. |
| `business/users/migration/AdminUserRecoveryServiceTest` | No | No | No | Not covered by higher-level tests. It verifies startup/recovery data migration behavior for initial admin creation; exercising this through UI/API would require specialized application bootstrap states and would be less direct. |
